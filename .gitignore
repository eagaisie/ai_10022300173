# Emmanuel Ato Gaisie
# 10022300173
# CS4241

# Python
__pycache__/
*.py[cod]
*$py.class
.Python
*.so

# Virtual environments
.venv/
env/
venv/
ENV/

# Hugging Face / transformers cache (large model weights — never commit)
.hf_cache/

# Local experiment logs and generated reports
logs/

# Generated chunk exports (rebuild with 1_data_prep.py)
data/all_chunks.json
data/csv_chunks.json
data/pdf_chunks.json

# Streamlit local secrets (never commit API keys)
.streamlit/secrets.toml

# Reports (Word etc. — keep local only; do not commit coursework write-ups)
*.docx
*.doc
~$*.docx
~$*.doc

# Auto-generated project report (local only — not for GitHub)
PROJECT_REPORT.md

# OS / editor noise
.DS_Store
Thumbs.db
*.swp
.idea/
.vscode/
